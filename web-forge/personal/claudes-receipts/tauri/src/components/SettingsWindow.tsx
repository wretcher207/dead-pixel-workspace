export default function SettingsWindow() {
  return (
    <div style={{ padding: "1.5rem", color: "var(--foreground)" }}>
      <h1 style={{ fontSize: "1rem", fontWeight: 600, margin: 0 }}>Settings</h1>
      <p>Settings UI — populated in Task 13.</p>
    </div>
  );
}
