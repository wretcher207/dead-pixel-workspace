# Handoff

## State
I built the Dead Pixel Toolbox site at `web-forge/toolbox-site/` — 10 working tools (JSON formatter, password generator, color palette, CSS gradient, base64 encoder, QR generator, image compressor, markdown preview, lorem ipsum, word counter). All plain HTML/CSS/JS, no framework. Deployed to https://deadpixel-toolbox.netlify.app. AdSense verification script (`ca-pub-1495111704142071`) is on all 11 pages. Site is verified in AdSense. SEO basics done (sitemap.xml, robots.txt).

## Next
1. Toggle auto ads ON in AdSense dashboard (David's action — Ads > By site)
2. Custom domain setup if David wants (e.g. tools.deadpixeldesign.com)
3. Add more tools or refine existing ones based on traffic data

## Context
- AMP auto-ads scripts David found won't work — pages are standard HTML, not AMP. Current adsbygoogle.js script is correct.
- Memory saved at `project_toolbox_site.md`
